# Menu_card
This Python program dynamically creates a menu card by accepting item names and prices as user input and formatting them with consistent spacing using string manipulation. Useful for learning input handling, loops, and string formatting in Python.
